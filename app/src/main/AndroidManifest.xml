<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hva.tsse.juniorleraar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TabActivity"
            android:label="@string/title_activity_tab"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:label="@string/app_name" android:name=".Dida"
            android:parentActivityName=".Home"
            android:theme="@style/Dida"/>
        <activity android:label="@string/app_name" android:name=".Colle"
            android:parentActivityName=".Home"
            android:theme="@style/Collegiaal"/>
        <activity android:label="@string/app_name" android:name=".Peda"
            android:parentActivityName=".Home"
            android:theme="@style/Peda"/>
        <activity android:label="Junior Leraar" android:name=".Info"
            android:parentActivityName=".Home" />

    </application>

</manifest>